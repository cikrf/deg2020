import { BallotTimerPipe } from './ballot-timer.pipe';

describe('BallotTimerPipe', () => {
  it('create an instance', () => {
    const pipe = new BallotTimerPipe();
    expect(pipe).toBeTruthy();
  });
});
